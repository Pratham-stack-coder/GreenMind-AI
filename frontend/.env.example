# GreenMind AI — Frontend Environment Variables
# If deploying on Vercel, set VITE_API_BASE_URL to your Render backend URL.
# If running locally with Vite proxy or Docker, this can be left blank (defaults to /api/v1).
VITE_API_BASE_URL=
